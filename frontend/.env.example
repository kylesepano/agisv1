# Leave this as /api for local development; Vite proxies it to Laravel.
# In Vercel, set the full Render API URL, including /api.
VITE_API_BASE_URL=/api

# Optional local proxy override.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
